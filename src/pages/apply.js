import styles from '../styles/Apply.module.css'
import Nav from '@/components/Nav/Nav'

const Apply = () => {
    return (
        <div>
            <Nav />
            <p>Apply </p>
        </div>
    )
}

export default Apply